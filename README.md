# Iran Missile

Clean Phaser + Vite starter.

## Scripts

- `npm run dev` starts the local Vite dev server.
- `npm run build` builds the project into `dist`.
- `npm run preview` previews the built project.
- `npm run deploy` builds and publishes `dist` with `gh-pages`.

## Structure

```text
.
├── index.html
├── package.json
├── package-lock.json
├── src/
│   └── main.js
└── vite.config.js
```
